name: Validate Repository S

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Simple check
        run: |
          echo "Hello, this is a test"
